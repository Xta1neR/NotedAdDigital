import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["assets.aceternity.com", "cdn.sanity.io", "images.unsplash.com", "storage.googleapis.com", "www.culturemonkey.io", "startup-template-sage.vercel.app"],

  },
};

export default nextConfig;
